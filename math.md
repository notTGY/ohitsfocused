So we need to fill 9:16 reel.
That is 9 width, 16 height.
Let's say 1 is the measure.

suppose we get boxes of size x times y
and z times w

first we resize first box to size 9 times y*9/x
second we resize second box to size 9 times w*9/z
now we only need one thing. we would expect that y*9/x + w*9/z = 16
but that not may be true always.
If we get number less than 16 - all good, we can just center the videos, we will get
some black left over space

if we get number bigger than 16 - we shrink the width instead to have black stripes on the sides of the video
